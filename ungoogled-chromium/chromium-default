# Default settings for ungoogled-chromium. This file is sourced by /bin/bash
# from the chromium launcher.

# Uncomment this to spoof timezone to UTC
#export TZ='Etc/UTC'

# Options to pass to ungoogled-chromium.
CHROMIUM_FLAGS="--enable-features=VaapiVideoDecoder"
CHROMIUM_FLAGS="${CHROMIUM_FLAGS} --disable-features=UseChromeOSDirectVideoDecoder"
CHROMIUM_FLAGS="${CHROMIUM_FLAGS} --gtk-version=4"
